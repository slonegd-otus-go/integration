syntax = "proto3";

option go_package = "grpc";

message SumArgs {
    int32 a = 1;
    int32 b = 2;
}

message SumResult {
    int32 d = 1;
}

service Calendar {
    rpc Sum(SumArgs) returns (SumResult);
};


